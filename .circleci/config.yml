version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run: |
         docker login -u $DOCKER_USER -p $DOCKER_PASS

     - run: docker build -t teresinahackerclube/cabure-pipeline .

     - run: docker push teresinahackerclube/cabure-pipeline:`git rev-parse HEAD | cut -c1-7`